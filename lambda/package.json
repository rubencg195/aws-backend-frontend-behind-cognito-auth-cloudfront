{
  "name": "lambda-api",
  "version": "1.0.0",
  "description": "Lambda function for authenticated API endpoints with Dog API integration",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws",
    "lambda",
    "api",
    "cognito",
    "authentication"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
